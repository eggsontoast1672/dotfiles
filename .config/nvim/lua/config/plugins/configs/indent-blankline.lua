local M = {
  "lukas-reineke/indent-blankline.nvim",
  config = {
    scope = {
      -- Remove the little line underneath the function definition
      enabled = false,
    },
  },
  main = "ibl",
}

return M
